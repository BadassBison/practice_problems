/**
 * addToEachWord
 *
 * When provided a list of words and a string, append the
 * string to each word in the list and return the new list
 */

const addToEachWord = (words, additionalString) => {
  // Write code here
}

// ex. [[paramArray], expectedResult]
const testCases = [
  // Write your test cases here
];

require('../test.js').test(addToEachWord, testCases);

